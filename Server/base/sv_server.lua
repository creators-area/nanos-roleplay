if NanosRP.UseLuaConfig then
    Server.SetName(NanosRP.Name, true)
    Server.SetDescription(NanosRP.Description, true)
    Server.SetMaxPlayers(NanosRP.MaxPlayers, true)
    Server.SetPassword(NanosRP.Password, true)
    Server.SetLogo(NanosRP.LogoURL, true)
end